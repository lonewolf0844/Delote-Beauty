<script>
  window.addEventListener('scroll', () => {
    if(window.scrollY > 0) {
      document.querySelector('.header').classList.add('scrolled')
    } else {
      document.querySelector('.header').classList.remove('scrolled')
    }
  })
</script>

<header class="header">
    <div class="container navbar">
        <!-- icon -->
        <div class="icon">
            <a href="">
                <img src="src/assets/icons/instagram.svg" alt="icon">
            </a>
        </div>

        <!-- menu -->
        <ul class="menu">
            <li class="menu-item">
                <a href="/" class="menu-link">Главная</a>
            </li>
            <li class="menu-item">
                <a href="/masters	" class="menu-link">Мастера</a>
            </li>
            <li class="menu-item">
                <a href="/price" class="menu-link">Косметика</a>
            </li>
        </ul>

        <!-- logo -->

        <div class="logo">
            <img src="src/assets/icons/logo(2).svg" alt="logo">
        </div>

          <!-- menu -->
          <ul class="menu">
            <li class="menu-item">
                <a href="/" class="menu-link">Акции</a>
            </li>
            <li class="menu-item">
                <a href="/" class="menu-link">Отзывы</a>
            </li>
            <li class="menu-item">
                <a href="/map" class="menu-link">Контакты</a>
            </li>
        </ul>

         <!-- icon -->
         <div class="icon">
            <img src="src/assets/icons/burger.svg" alt="icon">
        </div>
    </div>
</header>

<style lang="scss">
    @import "src/assets/styles/utils/var.scss";
 .header {
    width: 100%;
    position: fixed;
		z-index: 999;

		&.scrolled{
			background: rgba(0, 0, 0, 0.8);
			backdrop-filter: blur(10px);

			.navbar{
				padding: 10px 0px;
			}
		}
    

    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 30px 0;
        border-bottom: 1px solid rgb(255, 255, 255,0.5);
				transition: 0.5s;

        .menu {
            display: flex;
            gap: 80px;

            .menu-link {
                font-weight: 600;
                border-bottom: 1px solid transparent;
                transition: 0.5s;
                

                &:hover {
                    color: $brand;
                    border-bottom: 1px solid $brand;
                }
            }
        }

        .icon {
            cursor: pointer;
        }
    }
 }
</style>
